# archived/

`archived/` keeps the history of completed, cancelled, or superseded stories.

Archive for traceability, not for clutter. A story that no longer drives active
decisions should leave `active/`.

Starter-owned history may live under `archived/desultor/` if that keeps the
archive boundary clearer.
