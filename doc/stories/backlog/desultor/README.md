# backlog/desultor/

This subtree holds accepted but inactive work that belongs to Desultor itself
as a starter layer.

Use the root `backlog/` namespace for host-project work unless a separate
starter namespace is unnecessary.
