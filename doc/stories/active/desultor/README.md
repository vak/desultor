# active/desultor/

This subtree holds current active work on Desultor itself.

Keep it narrow. If starter work and host-project work both exist, separate them
by namespace instead of mixing them by default.
