# active/

`active/` should stay narrow.

The point of this bucket is current focus, not a warehouse of everything that
has ever remained unfinished.

If a reusable starter needs its own active work, keep it under a namespace such
as `active/desultor/` instead of mixing it with host-project work by default.
