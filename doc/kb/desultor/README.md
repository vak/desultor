# kb/desultor/

This subtree holds reusable notes that belong to Desultor itself as a starter.

If a note is about the host project rather than the starter layer, it should
live in the root `doc/kb/` namespace instead.
