# desultor/

This subtree holds starter-owned documents that do not naturally belong inside
`doc/spec/`, `doc/kb/`, `doc/rfc/`, or `doc/stories/`.

Use it for documents that describe Desultor itself as a reusable starter rather
than describing a host project that imported it.
